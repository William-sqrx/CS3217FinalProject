//
//  LevelViewModel.swift
//  AliceInBoredomLand
//
//  Created by daniel on 20/3/25.
//

import Foundation

class LevelViewModel: ObservableObject {
    // Links to GameEngine, is responsible for processing clicks to spawn heros
    // Up to you when the check to confirm the hero can be spawn in done
    // Note: Must separate Model from View entirely!

    /* @Published var gameEngine: GameEngine
    @Published var resources: Resources?

    init(gameEngine: GameEngine, resources: Resources) {
        self.gameEngine = gameEngine
        defer {
            resources = gameEngine.getResources()
        }
    } */
}
